{
  "name": "proptenant-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:backend": "cd backend && npm install --include=dev && npx prisma generate",
    "build:frontend": "cd frontend && npm install --include=dev && npm run build",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "dependencies": {
    "next": "^15.5.25",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.11",
    "typescript": "^5.4.5"
  }
}
